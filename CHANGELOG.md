# CoRead v2 更新日志

## v2.6.0 (2026-09-24)

### 🌐 新功能 · 中英双语界面（English UI）

- **界面语言切换**：设置页新增「语言 / Language」开关（中文 | EN），切换即时生效，书架与批注页自动重绘，无需重开插件
- **自动检测**：首次启动按系统语言选择（`zh*` → 中文，其余 → English），手动选择后记忆在 `cr-lang`
- **全量覆盖**：书架、导入提示、阅读器、目录/搜索/阅读设置面板、划线菜单、批注页与详情页、书签面板、AI 对话面板、配置提醒弹窗
- **分享卡片英文版**：小票风（Cashier / Item / TOTAL READERS / THANK YOU · PLEASE READ AGAIN）、卡片风（Made with CoRead）、邮票（COREAD POST · 8¢ / $1）；书名号在英文下改为弯引号 “ ”
- **AI 提示词随语言切换**：英文界面下发给 AI 的共读消息与上下文恢复摘要均为英文，避免 AI 用中文回复外语用户

### 🙋 共读称呼

- 设置页新增「共读称呼」：点击即可填写，留空 = 用户（默认），填「我 / I / me」自动切换为第一人称模板，填其他名字则按名字称呼，决定发给 AI 的消息里如何称呼你，例如「水盐选中了这段文字」「我的想法：」「你之前和我讨论过《沙丘》」，角色卡共读不再出戏
- 下方实时预览 AI 将收到的句式；名字最长 24 字，自动过滤换行与花括号
- 修正默认模板的人称混用（「用户选中了…」+「请帮我…」→ 统一为「请帮忙…」）
- 设置页顺序调整为 Language → AI Co-Read（关联记录 / 共读搭档 / 共读称呼）→ Theme → Ornaments，语言开关置顶便于外语用户首次找到
- 移除设置页中仅作展示、无法修改的「排版字号 / 行距 / 字体偏好」三行（实际调整请在阅读器内的阅读设置面板）

### 🗑️ 删除书籍

- **书架编辑模式**：书架顶栏新增「编辑」按钮，进入后每本书右上角出现 ×，点击即可删除；原长按删除保留为快捷方式
- **自绘确认弹窗**：替换原生 `confirm()`（部分 WebView 下不弹出，导致长按删除"没反应"），支持双语
- **删除更彻底**：同步清理该书的阅读进度、各章划线、全书划线汇总、笔记与「正在阅读」记录，删除后批注页不再残留该书划线（AI 批注文件暂保留）

### 🔧 结构调整

- 新增 `i18n.js`（翻译框架：`_t()` / `setLang()` / `applyI18n()`）与 `i18n_dict.js`（词典，格式 `key: ['中文', 'English']`，欢迎贡献其他语言）
- HTML 静态文案改用 `data-i18n` / `data-i18n-ph` / `data-i18n-title` 标记
- `index.ui.js` 提示词抽取为 `PROMPT_TEMPLATES`（占位符 `{bookTitle}` `{chapter}` `{text}` `{comment}` `{history}`），为下一版「自定义阅读提示词」预留接口
- Bridge 的 `sendToAI` / `sendFollowUp` 新增可选 `lang` 字段；缺省视为中文，兼容旧版 WebView 缓存
- 移除过时的 `MARKET_INTRO.md`

### 保留说明

- 竖排朱印「共讀 / 摘錄 / 設定」作为设计元素保留汉字
- TXT「第X章」分章、GBK 编码识别等中文书支持逻辑不变
- 工具子包（coread2_tools / coread2_config）返回值面向 AI，保持原样；其工具描述本已提供英文


## v2.5.2 (2026-09-02)

### 🔒 安全修复（响应市场审核）

- **Markdown 渲染 XSS**：`escHtml()` 与 `simpleMarkdown()` 的双引号转义此前为空操作（`"` → `"`），现正确转义为 `&quot;`，杜绝 `[x](x" onerror="…)` 式属性注入
- **URL 协议白名单**：新增 `safeUrl()`，`<a href>` 仅放行 `http/https/mailto/相对路径/锚点`，`<img src>` 额外放行 `data:image/(png|jpeg|gif|webp);base64`；`javascript:`、`vbscript:`、`data:text/html`、`data:image/svg+xml` 一律替换为 `#`。协议判定前会剥离控制字符并解码 `&amp;`，防大小写/制表符/实体绕过
- **链接 rel**：Markdown 链接统一附加 `rel="noopener noreferrer"`
- **代码块语言名**：`language-xxx` 仅保留 `[\w-]`，最长 32 字符
- **回归测试**：新增 27 条恶意/合法 Markdown 用例（`tests/xss_regression.js`），覆盖表格、代码块、Callout、行内链接/图片；全部通过

不含功能变更。EPUB 路径的 DOMParser 清洗逻辑未改动。

## v2.5.1 (2026-09-02)

> **重要说明**：市场上的 v2.4.2 因打包工具链问题，包内实际为 v2.3.1 的代码（详见下方「打包修复」）。v2.4.2 及以下版本用户请更新到本版本以获得 v2.4.x 全部修复与本版新功能。

### ✨ 新功能 · Markdown 渲染器重写

- **块级 Markdown 渲染器**：从逐行正则替换升级为完整的块级解析，安全策略不变（先整体转义 HTML 再解析语法）
- **表格**：支持 GFM 表格及列对齐（`:---` / `:---:` / `---:`），横向可滚动
- **围栏代码块**：` ``` ` 多行代码块，保留缩进与空行
- **Obsidian 语法**：Callout 块 `> [!note]` / `[!tip]` / `[!warning]` / `[!danger]` / `[!info]` / `[!example]` / `[!quote]`，各类型独立配色；WikiLink `[[页面]]`；嵌入 `![[文件]]`
- **扩展行内语法**：`==高亮==`、`~~删除线~~`、任务列表 `- [ ]` / `- [x]`、脚注 `^[...]`
- **分割线**：`---` / `***` 渲染为 `<hr>`
- **主题跟随**：所有 Markdown 扩展样式使用主题变量，深色/浅色/护眼模式下均正常显示

### 🐛 修复

- **换对话后上下文恢复失效（补完）**：`lastUsedChatId` 现持久化到 `_coread_config.json` 并在 `loadConfig()` 时恢复；`boot()` 不再无条件覆盖为当前 chatId，老版本配置文件缺少该字段时才回退为当前值。彻底修复"重开插件后换对话不触发上下文恢复"

### 📦 打包修复

- **发行包重新打包**：此前 v2.3.0～v2.4.3 及市场上的 v2.4.2 因工作区隐藏目录 `.backup/` 被打包工具带入、且市场混淆步骤以其中残留的旧 manifest 为根做依赖剪枝，导致包内仅剩 `.backup/_tmp_pkg_check2/`（v2.3.1 代码），根目录无 `manifest.json` 与 `dist/`。本版本改为在干净目录中打包并逐文件校验，包内结构：根目录 `manifest.json` + `dist/` + 文档，无隐藏目录。已向 Operit 上游反馈该工具链问题
- 发行包不再包含开发交接文档 `HANDOFF.md` 与 `.gitignore`

---


## v2.4.2 (2026-08-28)

### 🐛 修复 · 上下文恢复链路

- **换对话后恢复摘要永远不触发**（核心 bug）：原逻辑在发送消息时无条件更新 `__lastUsedChatId`，若触发时讨论历史尚未异步加载完，摘要未注入但标志已被消费，导致该对话之后永远收不到上下文恢复。现改为**只有真正注入了摘要才消费标志**（`maybeBuildRestorePrefix` 统一入口）
- **重开 CoRead 后"换过对话"的事实被抹平**：`boot()` 原先无条件 `__lastUsedChatId = __chatId`，配置文件里改了 chatId 也永远不触发注入。现 `lastUsedChatId` **持久化到 `_coread_config.json`**，跨插件重开保留；注入完成后落盘，重启不重复注入
- **恢复摘要回灌**：切换 chatId 时刻记录 `__chatSwitchAt` 时间戳，摘要只收录该时刻之前的历史，新对话中刚产生的记录不会被当作旧上下文回灌
- **历史竞态区分**：新增 `__historyLoaded` 标志，区分"历史尚未异步加载完"与"加载完但确实为空"（后者直接消费标志，不让其悬置）；文件不存在/读取失败同样视为加载完成
- **追问通道遗漏**：`sendFollowUp` 此前完全不注入恢复摘要，切换 chatId 后先追问会让新对话完全失去上下文，已接入统一恢复入口
- **AI 批注缓存 key 不一致**：`preloadAINotes` 推送缓存时误用未 sanitize 的原始 bookId 做 key（写入端用的是 safe），前后不一致时静默 miss，划线上 AI 批注点不显示
- **`get_highlights` 书名永远为空**：导出 JSON 中 `books[bid].title` 从未填充；现从 WebView 的 `window.currentBookTitle` 带出当前书的真实书名
- **摘要格式**：截断省略号按实际长度决定加不加（原文不足 60/80 字不再带 `...`）；全空条目跳过；历史为空时返回空串不注入标题头

### 🧹 代码整理

- 上下文恢复统一收敛到 `maybeBuildRestorePrefix()`，`sendToAI` 与 `sendFollowUp` 共用
- `buildContextSummary(bookTitle, cutoff)` 支持时间截断与书名兜底（追问场景从历史里找最近的书名）

### ✅ 验证

- 换对话后首次划线注入摘要 ✅ / 同对话后续不注入 ✅ / 重开 CoRead 不重复注入 ✅（实测通过）

---

## v2.4.1 (2026-08-25)

### 🔒 安全加固（审核修复）

- **EPUB HTML 解析改用 DOMParser**：不再通过 `innerHTML` 解析原始 EPUB HTML，杜绝解析阶段资源预加载风险
- **危险属性黑名单扩展**：新增 `src="javascript:"`、`xlink:href="javascript:"`、`action`、`formaction` 属性剥除；标签黑名单增加 `link[rel=import]`、`base`、`applet`
- **发行包结构修正**：确保 `.toolpkg` 归档根目录直接包含 `manifest.json` 和 `dist/`，不再嵌套子目录

---

## v2.4.0 (2026-08-25)

### ✨ 新功能

- **批注分享卡片 · 双样式系统**：分享按钮弹出样式选择（小票风 RECEIPT / 卡片风 CARD），记住上次选择
- **小票风分享卡**：热敏打印机质感——锯齿撕边、虚线分隔、生成式品名区与印花税票，"CoRead 共读书店 · 人机共读凭证"
- **卡片风分享卡 · Zine 杂志排版**：`COREAD ── No.xxxx` 刊头 + 居中大字摘录 + 锯齿撕边批注便签（微旋转贴纸感）+ 右下角斜盖圆形邮戳；白卡片 × 米色便签双色对比
- **印花税票盲盒**：每次导出随机生成——山水票（42%，含夜景星空版）/ 徽章票（36%）/ 普票三档纹理（22%），附随机面值与 COREAD 邮政铭记
- **预览与导出分离**：生成后先进入全屏纯预览，满意再点「导出保存」落盘，不满意直接关闭不产生文件；长按图片仍可分享到其他应用

### 🔒 安全加固（审核整改）

- **EPUB 内容 XSS 防护**：章节 HTML 注入前清除 script/iframe/object/embed/form 标签及全部事件属性、javascript: 链接；Markdown 渲染先整体转义再解析语法；书名/作者/章节标题等元数据统一 HTML 转义
- **book_id 全链路校验**：Bridge 的 onBookOpened、AI 批注预加载等入口全部接入白名单校验，杜绝路径穿越
- **打包结构修正**：修复 .toolpkg 压缩包根目录嵌套错误导致无法导入的问题

### 🛡️ 稳定性

- 章节文件缺失时按文件名模糊匹配 zip 内真实路径兜底，找不到时明确提示而非抛出异常
- 批注详情页层次感优化：弹层底色改为页面底色（bg-0），卡片浮起层次更清晰，跟随主题变量

---

## v2.3.1 (2026-08-23)

### 🔒 安全加固（审核整改）

- **路径穿越防护**：`get_highlights` / `add_annotation` / `remove_annotation` 的 `book_id` 参数新增白名单校验（仅允许字母、数字、下划线、连字符和点，限长 128），杜绝通过恶意 bookId 拼接任意文件路径；Bridge 端历史记录读写同样校验
- **移除 shell 命令调用**：全部文件操作改用结构化 `Tools.Files.read/write` API——配置读取、讨论历史读写、资源释放（原 mkdir/cat/cp）不再经过终端执行
- **分享卡导出路径收敛**：批注分享卡片 PNG 从公共 `/sdcard/Download/` 移入插件数据目录 `/sdcard/Download/Operit/CoRead2/`
- **WebView 权限调整**：关闭跨域文件访问（`allowUniversalAccessFromFileURLs`）；保留同源文件读取（AI 批注数据加载依赖此权限）

### 📄 文档

- 重写 README 为产品介绍风格（功能亮点、使用场景、快速上手、路线图）
- 新增市场介绍文档（MARKET_INTRO.md），含数据与权限说明

---

## v2.3.0

### ✨ 新功能

- **每本书独立对话历史**：讨论记录按书籍分开存储（`_coread_history_<bookId>.json`），切换书籍时自动加载对应历史，聊天面板打开即显示当前书的讨论
- **智能上下文注入**：仅在 chatId 变更时注入当前书的历史摘要（最近 8 条），同一对话内反复打开书籍不重复注入
- **翻页模式批注定位修复**：翻页模式下点击批注现在能准确跳转到对应页（遍历分页数组定位页码），滚动模式保持原有行为

---

## v2.2.0 (2026-08-21)

### ✨ 新功能

- **批注详情页**：批注总览页点击卡片进入详情页，完整展示原文引用、用户批注与 AI 批注（含角色卡名与自定义颜色圆点），支持一键跳转回原文位置
- **批注数量角标**：总览卡片右下角显示批注数（用户批注 + AI 批注合计），外层列表更清爽
- **AI 批注全量显示**：总览页与详情页现在能显示所有书籍的 AI 批注，不再局限于当前打开的书
- **批注分享卡片**（🧪 调试中）：详情页可生成主题化分享卡片并预览；自动保存通道尚在调试，当前请长按预览图手动保存
- **跳转精确定位**：详情页跳转原文后自动搜索定位到划线文字（滚动模式居中定位 / 翻页模式计算页码），定位成功后闪烁提示
- **进度保护**：批注跳转属于临时浏览，退出阅读器时自动还原原阅读进度，不会被跳转覆盖
- **批注删除**：批注总览页卡片右上角新增删除按钮，删除时同步清理全书高亮汇总、用户批注与当前章缓存

### 🐛 修复

- **子包命名冲突**：子包重命名为 `coread2_config` / `coread2_tools`，解决与市场其他插件无法共存的问题
- **配置读取失败**：`coread_config.js` 中 `Tools.Files.read()` 缺少 `await` 导致配置永远读不到，已修复为 async/await
- **短 ID 误判**：放宽 chatId 长度检测（`>= 30` → `>= 8`），兼容 18 位短 ID
- **chatId 常见错误拦截**：`set_coread_config` 新增 `validateChatId()`，拦截占位词（current/当前/test）、JSON 粘贴、带前缀、含空白等常见错误，并验证颜色格式
- **"桥接未就绪" + AI 批注不显示**：`boot()` 中桥接注册晚于页面加载导致 `window.CoreadBridge` 为 undefined；调整时序为先注册桥接再加载页面
- **Bridge 同步方法无法读文件**：新增 AI 批注异步预加载机制（`preloadAINotes` + 内存缓存 + `__coreadAINotesLoaded` 推送回调）
- **删除后跳回顶部**：删除批注改为单卡片淡出移除，不再整页重渲染，滚动位置保持不动
- **空书标签残留**：某本书划线删光后自动隐藏筛选标签并切回"全部"
- **删除按钮与文字重叠**：卡片右侧预留按钮空间
- **"书籍数据丢失"**：批注跳转时字符串 ID 与书架数字 ID 类型不匹配导致查不到书，已做类型兼容
- **跳转定位失败**：修复跳转与 `openBook` 内部进度恢复的竞态问题（轮询就绪 + 校验重试机制）

### 🎨 样式

- 批注详情页重设计：引号图标块 + 硬边框 + 偏移阴影，与批注总览卡片风格统一
- 用户批注与 AI 批注统一为同款卡片样式，以颜色区分（用户为墨色，AI 跟随配置的圆点颜色）
- 时间与批注角标间距优化

---

## v2.1.1

- 初始发布版本：EPUB/TXT/MD 阅读、划线批注、AI 共读讨论、翻页模式、批注总览
